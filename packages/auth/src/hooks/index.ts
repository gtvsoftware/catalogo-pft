/**
 * Hooks de autenticação
 *
 * Exporta hooks personalizados para gerenciamento avançado de sessão:
 * - useAuthSession: Hook principal para gerenciamento de sessão
 * - Funcionalidades: refresh, validação de expiração, estados de erro
 */

export { useAuthSession } from './use-auth-session'
